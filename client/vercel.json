{
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "build": {
    "env": {
      "CI": "false"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
